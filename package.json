{
  "name": "snake",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^12.0.0",
    "@testing-library/user-event": "^13.2.1",
    "ramda": "^0.27.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "web-vitals": "^2.1.0"
  },
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "test": "vitest",
    "preview": "docker build -t snake-app . && docker run -p 80:80 snake-app",
    "docker:preview": "docker build -t crvouga/snake-game:latest . && docker rm -f snake-game 2>/dev/null || true && docker run -p 8012:80 --name snake-game crvouga/snake-game:latest",
    "docker:push": "docker buildx build --platform linux/amd64,linux/arm64 -t crvouga/snake-game:latest --push ."
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "jsdom": "^26.1.0",
    "vite": "^6.3.2",
    "vitest": "^3.1.2"
  }
}